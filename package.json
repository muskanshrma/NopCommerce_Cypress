{
  "name": "nopcomm_pom",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cypress:open": "cypress open",
    "cy:run": "npx cypress run && cypress run --headed --spec \"cypress/integration/Tests/*.js\" --reporter mocha-allure-reporter ",
    "allure:report": "allure generate allure-results --clean -o allure-report",
    "test": "npm run cy:run && npm run launchReport || npm run posttest && npm run launchReport",
    "posttest": "npm run allure:report",
    "launchReport": "allure serve"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^9.7.0",
    "cypress-multi-reporters": "^1.6.1",
    "mocha": "^10.1.0",
    "mocha-allure-reporter": "^1.4.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.2.1",
    "mochawesome-report-generator": "^6.2.0"
  },
  "devDependencies": {
    "@shelex/cypress-allure-plugin": "^2.34.0",
    "cypress-mochawesome-reporter": "^3.2.3",
    "cypress-xpath": "^2.0.1"
  }
}